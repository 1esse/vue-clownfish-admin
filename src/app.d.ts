declare namespace AppGlobal {
  /**
   * 开关类型：on开 off关
   */
  type SwitchType = 'on' | 'off'

  /**
   * 环境类型 
   * 
   * development：开发环境；
   * staging：预发布环境；
   * production：生产环境；
   */
  type EnvType = 'development' | 'staging' | 'production'
}
